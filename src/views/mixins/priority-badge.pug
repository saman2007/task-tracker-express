mixin priority-badge(priority)
   span.badge(class=`badge-${priority}`)
     if priority === "HIGH"
       | 🔥 High
     else if priority === "MEDIUM"
       | ⚡ Medium
     else if priority === "LOW"
       | 🌱 Low